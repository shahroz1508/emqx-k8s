server {
    server_name mqtt.app.com;

    location / {
	#proxy_http_version 1.1;
	#proxy_set_header Upgrade $http_upgrade;
	#proxy_set_header Connection "Upgrade";
	#proxy_set_header Proxy "";
	#proxy_set_header Host $http_host;
	#proxy_set_header X-Real-IP $remote_addr;
	#proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
	#proxy_set_header X-Forwarded-Proto $scheme;
	#proxy_read_timeout 96000;
	proxy_pass http://IP:30080/;

    }

    listen 443 ssl; # managed by Certbot
    ssl_certificate /etc/letsencrypt/live/mqtt.app.com/fullchain.pem; # managed by Certbot
    ssl_certificate_key /etc/letsencrypt/live/mqtt.app.com/privkey.pem; # managed by Certbot
    include /etc/letsencrypt/options-ssl-nginx.conf; # managed by Certbot
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem; # managed by Certbot

}
server {
    if ($host = mqtt.app.com) {
        return 301 https://$host$request_uri;
    } # managed by Certbot


    server_name mqtt.app.com;

    listen 80;
    return 404; # managed by Certbot


}
